<!doctype html>
<html>
  <head>
    <title>Sinatra Grouppeto</title>
  </head>

<body>
  <header>
    <nav class="wrap">
    <% if logged_in? %>
     <p>Welcome, <%=current_user.username%>
     <a href="/rides/new">Create New Ride </a>
     <a href="/logout">Log Out</a>
     <a href="/rides">Show Up Coming Rids</a>
     <a href="/signup">Sign Up</a>
     <a href="/login">Login</a>
  <% end %>
  </nav>
</header>

 <flash>
 <% if flash.has?(:message)%>
      <%= flash[:message] %>
      <%end%>
    </flash>

    <%=yield%>

    </body>
 </html>
