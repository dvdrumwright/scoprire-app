<!doctype html>
<html>
  <head>
    <title>Sinatra Grouppeto</title>
  </head>

<body>
  <header>
    <nav class="wrap">
    <% if logged_in? %>
     <p>Welcome, <%=current_user.username%>
     <a href="/rides/show">Show Up Coming Rides</a>
  <% end %>
  </nav>
</header>

 <flash>
 <% if flash.has?(:message)%>
      <%= flash[:message] %>
      <%end%>
    </flash>

    <%=yield%>

    </body>
 </html>
