<h1>Cycling History </h1>

<ul>
  <% @rides.each do |ride| %>
  <li>
    <a href="/rides/<%= ride.id %>"><%= ride.description %> - <%= ride.ride_distance %></a>
  </li>
  <% end %>
</ul>

<p><a href="/rides/new">record a new ride</a></p>












<!-- <h3>Running History</h3>

<% @rides.each do |ride| %>
    <%= erb :'rides/_ride', locals: {username: ride.username, ride: ride} %>
<% end %> -->
